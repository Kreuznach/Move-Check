{
  "name": "move-check",
  "version": "1.0.0",
  "private": true,
  "description": "Static move checklist site",
  "scripts": {
    "dev": "serve . -l 4173",
    "build": "node scripts/build.mjs",
    "test": "node --test scripts/validate-build.test.mjs",
    "check": "npm run build && npm run test",
    "preview": "serve dist -l 4174"
  },
  "devDependencies": {
    "serve": "^14.2.4"
  }
}